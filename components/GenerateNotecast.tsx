import { GenerateNotecastProps } from "@/types";
import React from "react";

const GenerateNotecast = (props: GenerateNotecastProps) => {
  return <div>GenerateNotecast</div>;
};

export default GenerateNotecast;
